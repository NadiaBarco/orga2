global agrupar


;########### SECCION DE DATOS
section .data

;########### SECCION DE TEXTO (PROGRAMA)
section .text

;char** agrupar(msg_t *msgArr, size_t msgArr_len);
agrupar:
    push rbp
    mov rbp,rsp

    pop rbp

